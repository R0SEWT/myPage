---
const { title, desc, tags = [], href = '#' } = Astro.props;
---
<article class="group rounded-2xl border border-slate-200 dark:border-slate-800 p-5 hover:shadow-xl hover:shadow-teal/10 hover:-translate-y-1 transition-all duration-300 bg-white/70 dark:bg-slate-900/50 backdrop-blur hover:bg-white/90 dark:hover:bg-slate-900/70">
  <h3 class="font-semibold text-slate-900 dark:text-slate-100 group-hover:text-teal transition-colors">{title}</h3>
  <p class="mt-2 text-sm text-slate-600 dark:text-slate-300">{desc}</p>
  <div class="mt-3 flex flex-wrap gap-2 text-xs text-slate-500">
    {tags.map(t => 
      <span class="rounded-full border border-slate-200 dark:border-slate-800 px-2 py-0.5 group-hover:border-teal/30 group-hover:bg-teal/5 transition-colors">
        {t}
      </span>
    )}
  </div>
  <a href={href} target="_blank" rel="noopener" class="mt-4 inline-flex items-center gap-1 text-sm font-medium hover:text-teal transition-colors group">
    Ver más
    <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
    </svg>
  </a>
</article>
