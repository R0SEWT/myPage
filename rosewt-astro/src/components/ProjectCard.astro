---
const { title, desc, tags = [], href = '#' } = Astro.props;
---
<article class="rounded-2xl border border-slate-200 dark:border-slate-800 p-5 hover:shadow-md transition bg-white/70 dark:bg-slate-900/50 backdrop-blur">
  <h3 class="font-semibold">{title}</h3>
  <p class="mt-2 text-sm text-slate-600 dark:text-slate-300">{desc}</p>
  <div class="mt-3 flex flex-wrap gap-2 text-xs text-slate-500">
    {tags.map(t => <span class="rounded-full border border-slate-200 dark:border-slate-800 px-2 py-0.5">{t}</span>)}
  </div>
  <a href={href} target="_blank" rel="noopener" class="mt-4 inline-flex text-sm font-medium hover:text-teal">Ver más →</a>
</article>
