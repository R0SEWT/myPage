---
const { } = Astro.props;
---

<div class="glass rounded-2xl p-6">
  <h3 class="text-lg font-semibold mb-4">Envíame un mensaje</h3>
  <form 
    class="space-y-4" 
    name="contact" 
    method="POST" 
    data-netlify="true" 
    netlify-honeypot="bot-field"
    action="/gracias"
  >
    <input type="hidden" name="form-name" value="contact" />
    
    <!-- Campo anti-spam oculto -->
    <p style="display: none;">
      <label>
        Don't fill this out if you're human: 
        <input name="bot-field" />
      </label>
    </p>
    <div class="grid md:grid-cols-2 gap-4">
      <div>
        <label for="name" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">Nombre</label>
        <input 
          type="text" 
          id="name" 
          name="name"
          class="effect-base w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-800 px-3 py-2 text-sm focus:border-teal focus:ring-1 focus:ring-teal/20 focus:outline-none" 
          placeholder="Tu nombre"
          required
        />
      </div>
      <div>
        <label for="email" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">Email</label>
        <input 
          type="email" 
          id="email" 
          name="email"
          class="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-800 px-3 py-2 text-sm focus:border-teal focus:ring-1 focus:ring-teal/20 focus:outline-none effect-base" 
          placeholder="tu@email.com"
          required
        />
      </div>
    </div>
    <div>
      <label for="subject" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">Asunto</label>
      <input 
        type="text" 
        id="subject" 
        name="subject"
        class="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-800 px-3 py-2 text-sm focus:border-teal focus:ring-1 focus:ring-teal/20 focus:outline-none effect-base" 
        placeholder="¿De qué quieres hablar?"
        required
      />
    </div>
    <div>
      <label for="message" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">Mensaje</label>
      <textarea 
        id="message" 
        name="message"
        rows="3"
        class="w-full rounded-lg border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-800 px-3 py-2 text-sm focus:border-teal focus:ring-1 focus:ring-teal/20 focus:outline-none effect-base resize-none" 
        placeholder="Cuéntame sobre tu proyecto, idea o simplemente salúdame 👋"
        required
      ></textarea>
    </div>
    <div class="flex items-center justify-between">
      <p class="text-xs text-slate-500">
        📧 Respondo usualmente en 24-48 horas
      </p>
      <button 
        type="submit" 
        class="inline-flex items-center gap-2 rounded-lg bg-teal px-5 py-2 text-sm font-semibold text-white focus-ring effect-button focus:outline-none focus:ring-2 focus:ring-teal/50"
      >
        <span>Enviar</span>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
        </svg>
      </button>
    </div>
  </form>
</div>
